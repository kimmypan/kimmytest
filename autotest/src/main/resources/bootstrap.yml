server:
  port: 30101

spring:
  application:
    name: quickLoan-manager
    index: 0

  cloud:
    config:
      profile: dev
      failFast: true
      discovery:
        serviceId: config-server
        enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: "http://10.100.11.217:21111/eureka"

#encrypt:
#  keyStore:
#    location: classpath:/server.jks
#    password: "${KEYSTORE_PASSWORD:4DpeZbUL^Xy%KdHm}"
#    alias: config-server-key
#    secret: "oQ$!3QnXm%&cS%GQ"