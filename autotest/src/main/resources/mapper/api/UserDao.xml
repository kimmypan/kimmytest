<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.tdw.modules.api.dao.UserDao">

	<insert id="save" parameterType="cn.tdw.modules.api.entity.UserEntity" useGeneratedKeys="true" keyProperty="userId">
		insert into tb_user
		(
			`username`,
			`mobile`,
			`password`,
			`create_time`
		)
		values
			(
				#{username},
				#{mobile},
				#{password},
				#{createTime}
			)
	</insert>

	<select id="queryObject" resultType="cn.tdw.modules.api.entity.UserEntity">
		select * from tb_user where user_id = #{value}
	</select>

	<select id="queryByMobile" resultType="cn.tdw.modules.api.entity.UserEntity">
		select * from tb_user where mobile = #{value}
	</select>

	<select id="queryList" resultType="cn.tdw.modules.api.entity.UserEntity" parameterType="java.util.Map">
		SELECT userId, userName
		FROM tb_user
		WHERE 1=1
		<if test="userIds != null">
			AND <foreach collection="userIds" item="userId" open="(" separator="," close=")">#{userId}</foreach>
		</if>
	</select>

	<select id="queryByUserName" resultType="cn.tdw.modules.api.entity.UserEntity">
		SELECT user_id, userName
		FROM sys_user
		WHERE userName = #{value}
	</select>

</mapper>