<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.tdw.dao.FeedBackDAO">

	<select id="queryFeedBacks" resultType="cn.tdw.dto.FeedBackDTO">
		select
			feedBackId,tel_no,tel_brand,tel_model,os_version,app_version,content,status,createDate
		from FeedBack a
		<where>
			<if test="dto.startDate !=null and dto.startDate !='' ">
				and a.createDate >= #{dto.startDate}
			</if>
			<if test="dto.endDate !=null and dto.endDate !='' ">
				and a.createDate &lt; #{dto.endDate}
			</if>
		</where>
		ORDER BY createDate Desc


	</select>



</mapper>